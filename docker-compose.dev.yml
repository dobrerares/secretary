services:
  secretary:
    build:
      context: .
      target: deps
    volumes:
      - ./secretary:/app/secretary
      - secretary-data:/app/data
    command: uvicorn secretary.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - SECRETARY_LOG_LEVEL=DEBUG
